import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Willkommen bei BatzenOS!")
                .font(.title)
                .padding()
        }
    }
}
